# 迁移到 Vue：进度与分工

目标架构（商业化方向）：

| 端 | 技术栈 | 目录 | 状态 |
|---|---|---|---|
| 用户端 | Vue 3 + Vant | `web/` | 🟢 根目录唯一生产入口 |
| 管理后台 | Vue 3 + Element Plus | `admin/` | 🟢 可用 |
| 后端 | Cloudflare Worker + D1 | `analytics/` | 🟢 不变 |
| 旧站 | 单文件源码 | `src.html` / `build.mjs` | ⚪ 发布产物已删除，仅保留历史源码 |

旧站发布产物已下线。Vue 与旧站源码曾共用同一后端与 `data/`；现在生产只由 Vue
构建根目录产物提供。

> **方向决策（2026-08-13）**：Vue 是唯一继续演进的用户端；`src.html` 保留为视觉、
> 交互和数据行为的基准，以及可回退的历史实现。迁移不是重新设计：Vue 页面必须优先
> 复刻旧站的信息层级、导航位置、文案与交互节奏。任何上线仍须完成独立的验收与发布授权。

## 已迁移（11 个页面）

首页、短语、短文、**拼写练习**、排行榜（三榜切换）、我的、
登录 / 注册 / 重置密码（三页独立）、**隐私政策**、**支持作者**。

拼写练习复用旧站的 localStorage 键（`knownWords`/`spWrong`/`spSound`/`lastStudy`），
两端进度互通，迁移期不丢数据。

## 待迁移（按建议顺序）

1. 图解（body，旧站里也只有 0.6KB，内容由 JS 动态生成）
4. 徽章、打卡、学习数据同步到 /api/sync
5. Service Worker 离线（旧站有，新站尚无）
6. 拼写练习的 SRS 间隔复习（旧站有，本次只迁了核心闭环）
7. 测验的「变位」「数字」两种题型（需额外数据结构，现有四种已覆盖词汇与冠词）

## 两个 agent 的分工

`AGENTS.md` 第 2 节的原则不变，映射到新结构：

| 领域 | 目录 | 建议归属 |
|---|---|---|
| 内容数据 | `data/`、`dict/` | Codex |

> 注：对话数据原先硬编码在 `src.html` 里，已抽成 `data/dialogs.json` 供 Vue 端使用。
> 旧站仍读自己那份内嵌副本——迁移期两份并存，旧站下线后这份重复即自然消失。
> **改对话内容请改 `data/dialogs.json`**，旧站那份不再维护。
| 用户端组件 | `web/src/` | 按**视图文件**分工，一人一个 `.vue`，不要同改一个文件 |
| 后台 | `admin/src/` | 谁动谁负责 |
| 后端 | `analytics/` | 谁动谁负责，同步改 README 接口表 |
| 旧站 | `src.html` | 迁移期尽量冻结，只修 bug |

Vue 拆成多文件后，冲突面比 4124 行的 `src.html` 小得多——这本来就是迁移的主要收益之一。

## 双语支持

旧站把语言切换挂在顶栏、**所有页面都显示**，但 `readings`/`series`/`dialogs`
只有德语数据——用户切到英语后这些版块内容纹丝不动，是个真 bug。

Vue 端改为：**语言是内容维度，不是全局开关**。页面自己决定要不要引
`components/LangSwitch.vue`，没有英语数据的页面根本不出现这个控件。

| 页面 | 双语 | 数据 |
|---|---|---|
| 短语 | ✅ | `categories.json` / `en_categories.json` |
| 情景对话 | ✅ | `dialogs.json` / `en_dialogs.json` |
| 短文 | ✅ | `readings.json` / `en_readings.json` |
| 发音 / 数字 / 语法 | ✅ | `reference.json`（含 de/en 两份）|
| 连载 | ❌ 仅德语 | 待补 `en_series.json` |
| 拼写 / 测验 / 排行 | ❌ 不适用 | — |

朗读也跟着切（`de-DE` / `en-US`），切语言会退回列表页，避免停在另一语言的详情页上。

## 距离「Vue 全面替换旧站」还差什么

页面迁了 17/18，但**页面 ≠ 能力**。现在切过去用户会丢：

| 能力 | 旧站 | Vue 端 | 
|---|---|---|
| Service Worker 离线 | ✅ | ✅ 已补（外壳网络优先、数据缓存优先）|
| 徽章系统 | ✅ | ✅ 已补（14 枚徽章墙 + 解锁弹窗）|
| 打卡 / 连续天数 | ✅ | ✅ 已补（studyTick 与旧站算法逐字对齐）|
| 社交（关注/动态/他人主页）| ✅ | ✅ 已补 |
| 学习数据同步上榜 | ✅ | ✅ 已补（4 秒节流 + 页面隐藏时补推）|
| 图解版块 | ✅ | ❌ 旧站也仅 0.6KB，内容由 JS 生成 |
| 英语连载 | — | ❌ 无数据 |

**替换前还须**：验证老用户 localStorage 进度不丢、准备灰度与回退方案。

### 剩余差距（2026-08）
只剩两项非核心内容：图解版块（旧站 0.6KB，JS 动态生成）、英语连载（无数据）。
学习闭环、社交、离线、徽章均已对齐，具备灰度上线条件。

## 上线前必须做的（与技术栈无关）

1. 🔴 隐私政策 + 用户协议（站内 0 处，收手机号已违规）
2. 🔴 D1 备份机制（当前**无任何备份**，商业项目属致命风险）
3. 🔴 ICP 备案核实
4. ~~真机微信验证 legacy 包~~ —— **已取消**：站长 2026-08 决定不再支持微信，
   legacy 构建已移除，产物从 316KB(gz) 降到 121KB
