{
  "name": "deutsch-lernen-site",
  "version": "1.0.0",
  "private": true,
  "description": "德语学习手册（单文件）。改词只改 src.html，再 `npm run build` 生成加密混淆的 index.html。",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "terser": "^5.48.0"
  }
}
